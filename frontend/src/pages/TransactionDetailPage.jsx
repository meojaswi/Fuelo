import{useState}from"react";import{useParams}from"react-router-dom";import{useTransaction}from"../hooks/useTransactions";import TransactionInfoCard from"../components/transactions/TransactionInfoCard";import NotificationStatusCard from"../components/transactions/NotificationStatusCard";import RetryButton from"../components/transactions/RetryButton";import ShareLinkSection from"../components/transactions/ShareLinkSection";import{retryNotification}from"../api/notifications.api";export default function TransactionDetailPage(){const{id}=useParams(),{transaction:t,loading}=useTransaction(id),[busy,setBusy]=useState(false);if(loading)return <div className="py-16 text-center text-sm text-slate-400">Loading transaction…</div>;if(!t)return <p>Transaction not found.</p>;async function retry(){setBusy(true);try{await retryNotification(t.notificationId);location.reload()}finally{setBusy(false)}}return <div className="mx-auto max-w-3xl space-y-6"><div><p className="mono-label">Transaction / Detail</p><h2 className="mt-2 text-2xl font-bold">{t.customerName||"Transaction"}</h2><p className="mt-1 text-sm text-slate-500">Full record and customer delivery status.</p></div><TransactionInfoCard transaction={t}/><div className="grid gap-5 sm:grid-cols-2"><NotificationStatusCard status={t.notificationStatus}/><div className="panel p-5"><p className="mono-label">Delivery action</p><h3 className="mt-1 font-bold">Message delivery</h3><div className="mt-4"><RetryButton visible={t.notificationStatus==="failed"} onRetry={retry} loading={busy}/></div></div></div><ShareLinkSection transactionId={id}/></div>}