import{useState}from"react";import{Link}from"react-router-dom";import{Plus}from"lucide-react";import{useTransactions}from"../hooks/useTransactions";import FilterBar from"../components/transactions/FilterBar";import TransactionTable from"../components/transactions/TransactionTable";import TransactionCardList from"../components/transactions/TransactionCardList";import Pagination from"../components/shared/Pagination";import EmptyState from"../components/shared/EmptyState";export default function TransactionListPage(){const[f,setF]=useState({customer:"",itemTypes:[],page:1});const{items,pages,loading}=useTransactions({page:f.page,customer:f.customer,itemTypes:f.itemTypes.join(",")});return <div className="space-y-6"><div className="flex items-end justify-between"><div><p className="mono-label">Workspace history</p><h2 className="mt-2 text-2xl font-bold">Transactions.</h2><p className="mt-1 text-sm text-slate-500">Every recorded movement, in one place.</p></div><Link to="/transactions/new" className="hidden items-center gap-2 rounded-[8px] bg-fuelo-coral px-4 py-2.5 text-sm font-semibold text-white sm:flex"><Plus size={15}/> New</Link></div><div className="panel p-4"><FilterBar filters={f} setFilters={setF}/></div>{loading?<div className="py-16 text-center text-sm text-slate-400">Loading ledger…</div>:items.length?<><TransactionTable transactions={items}/><TransactionCardList transactions={items}/><Pagination page={f.page} pages={pages} onChange={page=>setF({...f,page})}/></>:<EmptyState title="No transactions found" description="Try a different search or record your first transaction."/>}</div>}