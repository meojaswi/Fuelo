import CustomerSearchInput from"./CustomerSearchInput";import ItemTypeFilter from"./ItemTypeFilter";export default function FilterBar({filters,setFilters}){return <div className="grid gap-4 md:grid-cols-[1fr_auto] md:items-end"><CustomerSearchInput value={filters.customer} onChange={customer=>setFilters({...filters,customer,page:1})}/><ItemTypeFilter value={filters.itemTypes} onChange={itemTypes=>setFilters({...filters,itemTypes,page:1})}/></div>}