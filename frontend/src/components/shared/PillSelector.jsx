export default function PillSelector({options,value,onChange,multiple=false}){const vals=multiple?(Array.isArray(value)?value:[]):[value];return <div className="flex flex-wrap gap-2">{options.map(o=>{const active=vals.includes(o);return <button type="button" key={o} onClick={()=>multiple?onChange(active?vals.filter(v=>v!==o):[...vals,o]):onChange(o)} className={`rounded-full border px-4 py-2 text-xs font-semibold transition ${active?"border-fuelo-ink bg-fuelo-ink text-white":"border-slate-200 bg-white text-slate-600 hover:border-slate-400"}`}>{o}</button>})}</div>}